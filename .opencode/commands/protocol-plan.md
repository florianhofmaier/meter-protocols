---
description: Plan a shared protocol-core change
agent: protocol-orchestrator
---

Plan the following shared protocol-core change:

$ARGUMENTS

Use the planning workflow:

- identify whether DLMS/COSEM, M-Bus or both are affected,
- delegate standard-sensitive assumptions to the relevant standard agent(s),
- produce an architecture/type/test plan,
- do not modify code.
